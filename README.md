# codex

这是一个当前几乎为空的 Git 仓库，当前仅包含 `.gitkeep`，可以把它看作项目初始化骨架。

## 仓库现状

- 分支：`work`
- 已有文件：
  - `.gitkeep`（用于保留空目录/仓库结构）
- 暂无业务代码、配置或测试。

## 后续维护约定（由 AI 协作维护）

为了便于后续我持续接手管理，这里先约定一套最小流程：

1. **先补基础结构**
   - `src/`：主代码
   - `tests/`：测试
   - `docs/`：文档
2. **每次改动都做简短变更说明**
   - 在 PR 描述里写清：改了什么、为什么改、如何验证。
3. **优先保持可运行、可回滚**
   - 小步提交；每次提交尽量只做一件事。
4. **README 持续更新**
   - 增加启动方式、目录结构、常见命令与排错说明。

## 下一步建议

你可以直接告诉我你要做的第一个目标，例如：

- 「初始化一个 Python/Node/Rust 项目骨架」
- 「加上 lint + test + CI」
- 「先做一个最小可运行 demo」

我会按这个 README 的约定继续推进。
